# Backend (Flask)

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
flask --app app run
```

## API prefix
- `/api/auth`
- `/api/products`
- `/api/admin`
- `/api/payment`
- `/api/ml`
